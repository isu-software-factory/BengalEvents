<% content_for :title, "Occasions" %>
<h1>Occasions</h1>
<hr>
<div class="container">
  <div class="row">
    <div class="col border-right">
      <div class="card-columns">
        <% @occasions.each do |occasion| %>
          <div class="card mb-5 border-dark">
            <div class="card-header">
              <h4> <%= occasion.name %>  </h4>
              <span>Created By: <%= occasion.coordinator.user.email %></span>
            </div>
            <div class="card-body text-dark">
              <p class="card-text">Name: <%= occasion.name %></p>
              <p class="card-text">Description: <%= occasion.description %></p>
              <p class="card-text">Start Date: <%= occasion.start_date.strftime("%Y-%m-%d") %></p>
              <p>
                <% if policy(occasion).edit? %>
                  <%= link_to "Edit", edit_event_path(occasion.id), class: "btn btn-secondary" %>
                <% end %>
                <% if policy(occasion).destroy? %>
                  <%= link_to "Delete", event_path(occasion.id), class: "btn btn-danger", data: {method: :delete, confirm: "Are you sure?"} %>
                <% end %></p>
              <hr>
              <p class="card-text">
                <button class="btn button-small" title="View list of events in occasion">
                  <span class="far fa-eye"></span><%= link_to "View Events", event_path(occasion.id), class: "btn button-small" %>
                </button>
              </p>
            </div>
          </div>
        <% end %>
      </div>
    </div>

    <div class="col-sm-2">
      <div class="card border-dark mb-3" style="max-width: 18rem;">
        <div class="card-header">Actions</div>
        <div class="card-body text-dark">
          <% if policy(@occasions).create? %>
            <button class="btn button-small" title="Create a new occasion">
              <span class="far fa-calendar-plus"></span><%= link_to "New Occasion", new_event_path %>
            </button>
          <% end %>
        </div>
      </div>
    </div>

  </div>
</div>


